version: "2"
sql:
  - engine: "postgresql"
    queries: "./database/query/*.sql"
    schema: "./database/schema"
    gen:
      go:
        package: "repository"
        out: "./repository"
        sql_package: "pgx/v5"
